extends ../layouts/commoncss
block content
    head
        style
            include ./css/animation.css
            include ./css/create.css
    body
        div(class="banner")
                h1(class="banner-head") Oops!!!!
        div(class="alert alert-danger")
            h3 we cannot find any users' profile with that ID

        //- p We cannot find any users' profile with that ID
        div(class="information pure-g")
                div(class="pure-u-1 pure-u-md-1-2")
                    div(class="l-box")
                        form(action="/users/search", method="get") 
                            h3(class="information-head") Please type again:
                            div(class="ani_form")
                                    input(type="text",name="q", autocomplete="off" required='' )
                                    label(for="name",class="ani_label-name")
                                        span(class="ani_content-name") Type your ID:
                            button(class="btn btn-info") Search
                        p
                        a(href="/users/create1") I'm a new user